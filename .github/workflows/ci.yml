name: CI

on: 
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CMake
        run: cmake --preset release
      - name: Build
        run: cmake --build --preset release

